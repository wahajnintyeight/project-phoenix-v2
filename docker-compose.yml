version: "1"
services:
  api-gateway:
    build:
      dockerfile: Dockerfile
      context: ./pkg/service/api-gateway
    ports:
      - "8889:8889"
  location-service:
    build:
      dockerfile: Dockerfile
      context: ./pkg/service/location-service
    ports:
      - "8882:8882"
