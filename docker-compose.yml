version: "3.8"
services:
  api-gateway:
    build:
      dockerfile: pkg/service/apigateway/Dockerfile
      context: .
    ports:
      - "8901:8901"
  location-service:
    build:
      dockerfile: pkg/service/locationservice/Dockerfile
      context: .
    ports:
      - "8882:8882"
