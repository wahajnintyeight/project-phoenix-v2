version: "1"
services:
  api-gateway:
    build:
      dockerfile: pkg/service/api-gateway/Dockerfile
      context: .
    ports:
      - "8889:8889"
  location-service:
    build:
      dockerfile: pkg/service/location-service/Dockerfile
      context: .
    ports:
      - "8882:8882"
